#include <bits/stdc++.h>

using namespace std;

int main(){
    cout<<"Enter the message"<<"\n";
    string s,result;
    cin>>s;
    int half=(s.size()+1)/2;
    for(int i=0;i<half;i++){
        result.push_back(s[i]);
        result.push_back(s[half+i]);
    }
    cout<<result;

    return 0;
}
